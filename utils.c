#include "utils.h"

char *generarPalabra(int size){
    // alocar espacio para 100 palabras, cada una de tamaño size bytes
    char **resp = calloc(100, size);
    for(int i = 0; i < size, i++){
        
    }
}